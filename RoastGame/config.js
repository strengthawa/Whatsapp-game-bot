// ============================================================
//  RoastGame/config.js — Roast Game · Sky Graphics
//  Single source of truth for this game's identity + prefixes.
// ============================================================

module.exports = {
    GAME_KEY:      'roast',
    GAME_NAME:     'Roast Game',
    GAME_ACRONYM:  'RST',

    // Public command prefix (e.g. "!roast me")
    PREFIX:        '!roast',
    // Admin command prefix — trailing space, matches project convention
    ADMIN_PREFIX:  '/roast ',

    // ── Shared brand identity (every "card" message) ──────────
    BOT_EMOJI:  '🔥',
    DIVIDER:    '━━━━━━━━━━━━━━━━━━━━━━'
}
